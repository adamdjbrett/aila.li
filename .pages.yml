content:
  - name: urls
    label: Shortlinks
    path: content/urls
    type: collection
    filename: '{primary}.md'
    fields:
      - { name: title, label: Title, type: string, required: true }
      - { name: url, label: Destination URL, type: string, required: true }